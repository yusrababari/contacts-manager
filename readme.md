# Contacts Manager

Languages: Python 

Imports JSON data file which contains a  contacts list into Python.

The Python file contains code which parses throught the JSON file and retrieves specific pieces of data.

To-do

. Implement dictionary

. Implement array

.Wrap JSON file in an exception handler